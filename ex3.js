// Faça um programa que imprima os números ímpares de 0 a 50;

// Fot (inicializacao; condicao; contador +=2)
// Fot (inicializacao; condicao; contador = contador + 2)
for (let contador = 1; contador <= 100; contador += 2) {
    console.log(contador)
}
// Calcular a soma dos números de 1 a 100 utilizando a estrutura while.
let contador = 0
let soma = 0

while (contador <= 5) {

    soma = soma + contador;
    // soma+= contador

    console.log(soma)

    contador++
}